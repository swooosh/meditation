# RECON-03 - Shadow Integration

## Overview
- Family: `RECON`
- Duration: `40m`
- ID: `RECON-03`

## Intended Use
Use `RECON-03` as a structured 40-minute session. Run in a quiet setting with headphones.

## Phase Structure
- Phase 1: `Ground` (`0s` to `600s`)
- Phase 2: `Explore` (`600s` to `1800s`)
- Phase 3: `Integrate` (`1800s` to `2400s`)

## Audio Layers
- `noise_bed`
- `binaural`
- `harmonic_pad`
- `pulse_cues`

## Render
```bash
cd /Users/jdarrow/workspace/meditation/protocol-lab/tools
python3 render_protocol.py ../protocols/RECON/specs/RECON-03_40m.yaml --out_dir ../protocols/RECON/renders
```

## Narration
- Coaching script: `/Users/jdarrow/workspace/meditation/protocol-lab/protocols/RECON/narration/RECON-03_coaching.md`

## Safety
- Do not use while driving or operating machinery.
- Stop immediately if distress escalates.
- Use conservative volume levels.
